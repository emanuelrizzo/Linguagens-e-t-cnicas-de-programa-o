#include <stdio.h>
#include <stdlib.h>

int main() {

int base, altura; 
float area;

base = 10;
altura = 20;

area = (base*altura)/2;

printf ("A area do Triangulo Retangulo de Base = %d e Altura = %d e %f",base,altura,area);

	return 0;
}
